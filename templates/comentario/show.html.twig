{% extends 'base.html.twig' %}

{% block title %}Comentario{% endblock %}

{% block body %}
    <h1>Comentario</h1>

                <p>{{ comentario.content }}</p>
                <p>{{ comentario.datePublic ? comentario.datePublic|date('Y-m-d H:i:s') : '' }}</p>


    <a href="{{ path('comentario_index') }}"> Pagina Principal </a>

    <a href="{{ path('comentario_edit', {'idComentario': comentario.idComentario}) }}">edit</a>

    {{ include('comentario/_delete_form.html.twig') }}
{% endblock %}
