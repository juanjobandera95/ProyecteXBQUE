<form method="post" action="{{ path('usuarios_delete', {'idUser': usuario.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.idUser) }}">
    <button class="btn">Delete</button>
</form>
